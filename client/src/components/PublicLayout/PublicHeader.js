import * as React from "react";

import PublicAppBar from './PublicAppBar';

const PublicHeader = () => {
  return (
    <React.Fragment>
      <header>
        <PublicAppBar />
      </header>
    </React.Fragment>
  );
};

export default PublicHeader;
